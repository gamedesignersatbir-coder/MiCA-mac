# Workflows

Recommended workflow for skill development:

1. **Ideation**: Define the problem and tool surface.
2. **Setup**: Use `scripts/init_skill.py` to create the structure.
3. **Implementation**: Build your tools and core logic.
4. **Validation**: Run `scripts/quick_validate.py`.
5. **Packaging**: Run `scripts/package_skill.py`.
